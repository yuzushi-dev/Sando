{
  "id": "read-large",
  "description": "Repeated source reads with a stable large prefix and a small changing tail.",
  "events": [
    {
      "id": "read-1",
      "toolName": "Read",
      "path": "src/service.mjs",
      "requiredFacts": ["return input.trim();"],
      "output": "// fixture source\nexport function service(input) {\n  return input.trim();\n}\n"
    },
    {
      "id": "read-2",
      "toolName": "Read",
      "path": "src/service.mjs",
      "requiredFacts": ["changed tail"],
      "output": "// fixture source\nexport function service(input) {\n  return input.trim();\n}\n// changed tail\n"
    }
  ]
}
